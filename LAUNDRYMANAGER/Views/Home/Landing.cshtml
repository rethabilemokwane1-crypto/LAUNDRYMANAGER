@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>iWS Laundry Portal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #12996b, #0d6e4d);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .landing-card {
            background: #fff;
            border-radius: 20px;
            padding: 2.5rem;
            max-width: 420px;
            text-align: center;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
        }

        .icon-circle {
            width: 72px;
            height: 72px;
            border-radius: 50%;
            background: #e6f7f1;
            color: #12996b;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            margin: 0 auto 1.25rem;
        }
    </style>
</head>
<body>
    <div class="landing-card">
        <div class="icon-circle">
            <i class="bi bi-water"></i>
        </div>
        <h2 class="fw-bold mb-2">iWS Laundry Portal</h2>
        <p class="text-muted mb-4">
            Check machine availability, book a washer or dryer, and track your cycle
            in real time — all from your phone.
        </p>

        <div class="d-grid gap-2 mb-3">
            <a href="/Account/Login" class="btn btn-success btn-lg fw-bold">
                <i class="bi bi-box-arrow-in-right me-2"></i>Log In
            </a>
            <a href="/Account/Register" class="btn btn-outline-success btn-lg fw-bold">
                <i class="bi bi-person-plus me-2"></i>Create Account
            </a>
        </div>

        <p class="text-muted small mb-0">
            Forgot your password?
            <a href="/Account/ForgotPassword" class="text-success fw-bold">Reset it here</a>
        </p>
    </div>
</body>
</html>